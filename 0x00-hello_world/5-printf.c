#include <stdio.h>
/**
 * main - This is the main function where execution begins
 *
 *
 * Return - The return statement returns 0 as an integer
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
