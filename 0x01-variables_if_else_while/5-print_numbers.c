#include <stdio.h>

/**
 * main - This is the main function that contains the whole program
 *
 * Return: This returns 0 to the int main function
 *
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
